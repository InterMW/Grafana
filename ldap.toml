[[servers]]
host = "10.0.0.5"
port = 389
use_ssl = false
start_tls = false
ssl_skip_verify = false

bind_dn = "ou=people,dc=inter,dc=lan"

timeout = 10

search_filter = "(uid=%s)"


search_base_dns = ["dc=inter,dc=lan"]
bind_password = 'pass'

[server.attributes]
username = "cn"

[[servers.group_mappings]]
group_dn = "ou=People,dc=inter,dc=lan"
org_role = "Admin"
grafana_admin= true
